using UnityEngine;
using UnityEditor;

namespace Experilous.WrapAround
{
	/*[CustomEditor(typeof(RhomboidWorld))]
	public class RhomboidWorldEditor : Editor
	{
		public enum AxisStyles
		{
			UnitOrthogonal,
			Orthogonal,
			Independent,
		}

		public override void OnInspectorGUI()
		{
			var world = (RhomboidWorld)target;

			var axisStyle = (AxisStyles)EditorGUILayout.EnumPopup(
		}
	}*/
}
